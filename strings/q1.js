//prompt the user to enter their full name. generate a username for them based on the input. start username with @, followed by their full name and enfing with the fullname length.

let name = prompt("enter your full name");

let username = "@ + name + name.length ;

console.log(username);